// Generated by CoffeeScript 1.3.3
(function() {

  define(["jquery", "./viewmodels/UserViewModel"], function($, UserViewModel) {
    return {
      refreshListview: function(element) {
        element.listview("refresh");
        return $(document).trigger("updatelayout");
      },
      addListviewItem: function(list, text, cssclass) {
        return list.append($("<li>").append($("<a/>")).addClass(cssclass ? cssclass : "\"friendclick\">").append(text));
      },
      getRoomName: function(remoteusername) {
        var username;
        username = UserViewModel.getUsername();
        if (username < remoteusername) {
          return username + "_" + remoteusername;
        } else {
          return remoteusername + "_" + username;
        }
      }
    };
  });

}).call(this);
